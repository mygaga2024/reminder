services:
  reminder:
    build: .
    image: ghcr.io/mygaga2024/reminder:latest
    container_name: life-reminder
    restart: unless-stopped
    ports:
      - "35015:5000"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
      - PUID=0
      - PGID=0
      - UMASK=000
      - APP_PORT=5000
      - DATA_DIR=/app/data
      # 极空间(ZSpace)兼容策略始终启用，无需额外开关（ZSPACE_COMPAT 为历史占位项）
      # API Key 认证（可选）
      # - API_KEY=your-secret-key-here
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
